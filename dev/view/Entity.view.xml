<mvc:View controllerName="devepos.qdrt.controller.Entity"
    xmlns:app="devepos.qdrt.control"
    xmlns:l="sap.ui.layout"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:f="sap.f"
    xmlns:table="sap.ui.table"
    xmlns:core="sap.ui.core"
    xmlns="sap.m"
    height="100%">

    <f:DynamicPage fitContent="true"
        class="sapUiNoContentPadding">
        <f:title>
            <f:DynamicPageTitle>
                <f:heading>
                    <Title text="Entity {/entity/name}"></Title>
                </f:heading>
                <f:actions>
                    <ToggleButton icon="sap-icon://customize"
                        pressed="{ui>/sideContentVisible}"
                        type="Transparent" />
                </f:actions>
            </f:DynamicPageTitle>
        </f:title>

        <f:content>
            <app:ToggleableSideContent sideContentVisible="{ui>/sideContentVisible}"
                sideContentPosition="End">
                <app:content>
                    <Panel height="100%">
                        <table:Table id="dataPreviewTable"
                            columns="{
                                path: '/columnMetadata',
                                factory: '.columnsFactory'
                            }"
                            rows="{/rows}"
                            visibleRowCountMode="Auto">
                            <table:extension>
                                <OverflowToolbar>
                                    <Title text="Data for {/entity/name}" />
                                    <ToolbarSpacer />
                                    <Button type="Transparent"
                                        press="onTableSettings"
                                        icon="sap-icon://action-settings"></Button>
                                    <Button text="{i18n>updateData}"
                                        icon="sap-icon://refresh"
                                        type="Emphasized"
                                        press="onUpdateData" />
                                </OverflowToolbar>
                            </table:extension>
                        </table:Table>
                    </Panel>
                </app:content>
                <app:sideContent>
                    <l:VerticalLayout width="100%">
                        <Toolbar >
                            <ToolbarSpacer />
                            <Button icon="sap-icon://back-to-top"
                                type="Transparent" />
                            <Button icon="sap-icon://delete"
                                type="Transparent" />
                        </Toolbar>
                        <Panel headerText="Filters"
                            width="100%"
                            expandable="false">
                            <!-- TODO: Determine correct max height -->
                            <ScrollContainer vertical="true"
                                height="500px">
                            </ScrollContainer>
                        </Panel>
                    </l:VerticalLayout>
                </app:sideContent>
            </app:ToggleableSideContent>
        </f:content>
    </f:DynamicPage>
</mvc:View>
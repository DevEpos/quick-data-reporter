import Item from "sap/ui/core/Item";

/**
 * Holds configuration for a DB entity filter
 *
 * @namespace com.devepos.qdrt.type
 */
export default class FilterItem extends Item {
    metadata = {
        properties: {}
    };
}

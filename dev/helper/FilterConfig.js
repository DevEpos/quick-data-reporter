import Element from "sap/ui/core/Element";

/**
 * Holds configuration for a DB entity filter
 *
 * @namespace devepos.qdrt.type
 */
export default class FilterConfig extends Element {
    metadata = {
        properties: {}
    };
}
